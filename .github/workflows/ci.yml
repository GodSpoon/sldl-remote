name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

env:
  CARGO_TERM_COLOR: always

jobs:
  check:
    name: Check & Test
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: sldl-remote/package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-action@stable
        with:
          toolchain: stable

      - name: Install Tauri CLI
        run: cargo install tauri-cli --locked

      - name: Install frontend dependencies
        working-directory: sldl-remote
        run: npm install

      - name: Check Rust formatting
        working-directory: sldl-remote/src-tauri
        run: cargo fmt -- --check

      - name: Run Clippy
        working-directory: sldl-remote/src-tauri
        run: cargo clippy -- -D warnings

      - name: Run Rust tests
        working-directory: sldl-remote/src-tauri
        run: cargo test

      - name: Build frontend
        working-directory: sldl-remote
        run: npm run build

      - name: Build Tauri app
        working-directory: sldl-remote
        run: cargo tauri build --no-bundle

  build:
    name: Build Release Bundle
    runs-on: macos-latest
    needs: check
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: sldl-remote/package-lock.json

      - name: Setup Rust
        uses: dtolnay/rust-action@stable
        with:
          toolchain: stable

      - name: Install Tauri CLI
        run: cargo install tauri-cli --locked

      - name: Install frontend dependencies
        working-directory: sldl-remote
        run: npm install

      - name: Build release bundle
        working-directory: sldl-remote
        run: cargo tauri build
        env:
          TAURI_SIGNING_PRIVATE_KEY: ""
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ""

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: sldl-remote-macos
          path: |
            sldl-remote/src-tauri/target/universal-apple-darwin/release/bundle/**/*.app
            sldl-remote/src-tauri/target/universal-apple-darwin/release/bundle/**/*.dmg
            sldl-remote/src-tauri/target/x86_64-apple-darwin/release/bundle/**/*.app
            sldl-remote/src-tauri/target/x86_64-apple-darwin/release/bundle/**/*.dmg
            sldl-remote/src-tauri/target/aarch64-apple-darwin/release/bundle/**/*.app
            sldl-remote/src-tauri/target/aarch64-apple-darwin/release/bundle/**/*.dmg
          if-no-files-found: ignore
          retention-days: 7
